# Semaphore Configuration
# Copy to .env and configure

# Admin credentials (auto-generated by bootstrap.sh if not set)
SEMAPHORE_ADMIN_NAME=admin
SEMAPHORE_ADMIN_EMAIL=admin@homelab.local
SEMAPHORE_ADMIN_PASSWORD=CHANGE_ME

# Access key for encryption (auto-generated by bootstrap.sh if not set)
SEMAPHORE_ACCESS_KEY=CHANGE_ME

# Web host URL
SEMAPHORE_WEB_HOST=http://192.168.0.199:3001

# Data directory (created by bootstrap.sh)
SEMAPHORE_DATA_DIR=/opt/semaphore

# Repository path (mounted read-only)
REPO_PATH=/home/code/home

# Timezone
TIMEZONE=Europe/Warsaw
