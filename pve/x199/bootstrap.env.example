# Control Node Bootstrap Configuration
# Copy to .env and configure before running ./bootstrap.sh

# =============================================================================
# REQUIRED - Must be configured
# =============================================================================

# Cloudflare API credentials (for DNS challenge / wildcard certs)
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_EMAIL=

# Base domain for services (e.g., example.com)
BASE_DOMAIN=

# Subdomains (combined as SUBDOMAIN.BASE_DOMAIN)
SEMAPHORE_SUBDOMAIN=semaphore.local
WEBHOOK_SUBDOMAIN=webhook

# Control node IP (for DNS instructions)
CONTROL_NODE_IP=

# Local network range for Caddy IP whitelist (CIDR)
LOCAL_NETWORK_RANGE=192.168.0.0/24

# =============================================================================
# OPTIONAL - Will use defaults or auto-generate
# =============================================================================

# Repository settings (files are synced to ~/)
REPO_PATH=${HOME}
REPO_BRANCH=main

# Semaphore data dir base (actual path: ${SEMAPHORE_DATA_DIR}/.semaphore/)
SEMAPHORE_DATA_DIR=${HOME}
SEMAPHORE_ADMIN_NAME=admin
SEMAPHORE_ADMIN_EMAIL=admin@homelab.local
# SEMAPHORE_ADMIN_PASSWORD=        # Auto-generated if not set
# SEMAPHORE_ACCESS_KEY=            # Auto-generated if not set

# Webhook settings
# GITHUB_WEBHOOK_SECRET=           # Auto-generated if not set

# Ansible settings
# SSH_KEY_NAME=id_ed25519          # Set to generate SSH key for Ansible

# Managed nodes for SSH key distribution (space-separated IPs)
# MANAGED_NODES="192.168.0.201 192.168.0.202"

# Timezone
TIMEZONE=Europe/Warsaw
