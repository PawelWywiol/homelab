# Remote sync configuration for x000
# Copy this file to .envrc and fill in REMOTE_HOST

REMOTE_HOST=""
REMOTE_FILES=(
  ".env"
  "docker/config"
  "Makefile"
  "ansible"
  "infra"
  "bootstrap.sh"
  "bootstrap.env.example"
  "backup-control-node.sh"
  "verify-backups.sh"
)
