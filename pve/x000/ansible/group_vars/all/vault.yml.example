---
# Ansible Vault - Encrypted Secrets Template
# Currently unused - services use .env files for secrets
# Keep for future use if templating .env files via Ansible
#
# Usage:
#   cp vault.yml.example vault.yml
#   ansible-vault encrypt vault.yml
