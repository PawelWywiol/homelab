# init-host.sh configuration
# Copy to .env and modify as needed

# Username to create
USERNAME="code"

# SSH authorized keys to add (one per line, or space-separated)
# Leave empty to skip
AUTHORIZED_KEYS=""
